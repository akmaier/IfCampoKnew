# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Node (for a future scraper / build tooling)
node_modules/
dist/
build/
*.log
.env
.env.local

# Python (scraper)
__pycache__/
*.pyc
.venv/
venv/
scraper/.venv/

# Local scratch / exploration — including JSON intermediates from the scraper
tmp/
scratch/
*.local.*

# Scraper's JSON intermediate (the deliverable is the markdown corpus under data/{period}/)
data/*.json

# Claude Code local config (kept out of git)
.claude/
